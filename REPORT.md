# 图神经网络 Beginner 实验报告

## 1. 环境与复现信息

- 运行环境：AutoDL `graph` Conda 环境，Python 3.11.16。
- 计算设备：NVIDIA RTX 4090 D，CUDA 可用。
- 主要依赖：PyTorch 2.7.1+cu128、PyTorch Geometric 2.8.0.post1、NumPy 2.4.6、scikit-learn 1.9.0。
- 采样扩展：pyg-lib 0.5.0、torch-scatter 2.1.2、torch-sparse 0.6.18，均与 PyTorch 2.7/CUDA 12.8 对应。
- `pip check` 无依赖冲突；`python self_check.py` 输出 `SELF_CHECK_OK`。
- 所有实验固定随机种子为 42，自动使用 CUDA，指标保存为 JSON。

快速复现：

```bash
conda activate graph
python self_check.py
bash run_quick_experiments.sh
python run_ablation.py --epochs 30
```

## 2. 方法与实现

| 任务 | 数据与划分 | 模型/方法 | 指标 |
|---|---|---|---|
| 节点分类 | Cora、Citeseer、Flickr 的官方掩码 | GCN、GAT、GraphSAGE、GIN；全图与 NeighborLoader | 验证/测试 Accuracy、耗时 |
| 链路预测 | RandomLinkSplit 划分训练/验证/测试边并生成负边 | 四种 GNN 编码器、点积解码器；全图与 LinkNeighborLoader | 验证/测试 ROC-AUC、耗时 |
| 图分类/回归 | TUDataset 按 8:1:1 划分；ZINC 使用官方 split | 四种 GNN；Avg/Max/Min 全局池化 | Accuracy 或 MAE、耗时 |
| 知识图谱补全 | 独立 train/valid/test TSV | TransE、RotatE、ConvE；头/尾实体负采样 | 双向 filtered MRR、Hits@K、Mean Rank |

知识图谱评测同时计算 `(?, r, t)` 与 `(h, r, ?)`，过滤训练、验证和测试集中其余已知真三元组。内置 toy KG 的测试三元组不出现在训练集中，避免数据泄漏。

## 3. 已运行实验范围

用于模型对比的正式基线为：Cora 节点分类 30 轮、Cora 链路预测 30 轮、MUTAG 图分类 30 轮、toy KG 300 轮。另完成 Citeseer 节点分类、Citeseer 链路预测和 PROTEINS 图分类的一轮数据加载/训练自检。

Flickr 的 Google Drive 数据源和 ZINC 的 Dropbox 数据源在本次 AutoDL 网络上连接超时，因此这两个数据集的代码与命令已提供，但没有将未完成的下载记作实验结果。Cora/Citeseer 改用可访问镜像下载原始 Planetoid 文件，数据格式仍由 PyG 官方处理器解析。

## 4. 实验结果

### 4.1 Cora 节点分类（30 轮）

| 模型 | 全图 Test Acc | 采样 Test Acc | 全图秒/轮 | 采样秒/轮 |
|---|---:|---:|---:|---:|
| GCN | 0.816 | **0.826** | 0.0129 | **0.0031** |
| GAT | **0.693** | 0.668 | 0.0050 | 0.0048 |
| GraphSAGE | **0.802** | 0.794 | **0.0022** | 0.0028 |
| GIN | 0.656 | **0.709** | **0.0026** | 0.0032 |

GCN 在本设置下准确率最高。采样并未明显损害结果：GCN 和 GIN 的采样结果反而略高，但这可能来自随机优化差异。Cora 很小，采样器的固定开销占比较高；除包含首次 CUDA 热身的 GCN 全图计时外，其他模型的全图训练通常与采样相当或更快。因此，子图采样的主要优势应在 Flickr 等更大图上体现，而不是 Cora。

### 4.2 Cora 链路预测（30 轮）

| 模型 | 全图 Test AUC | 采样 Test AUC | 全图秒/轮 | 采样秒/轮 |
|---|---:|---:|---:|---:|
| GCN | **0.745** | 0.741 | **0.0131** | 0.0225 |
| GAT | **0.504** | 0.500 | **0.0048** | 0.0298 |
| GraphSAGE | **0.723** | 0.710 | **0.0021** | 0.0195 |
| GIN | 0.641 | **0.715** | **0.0026** | 0.0218 |

GCN 全图的 AUC 最高，GraphSAGE 次之。GIN 对采样训练更适应，而 GAT 在当前点积解码器和短训练设置下接近随机水平，需要调节注意力头数、学习率或采用更强的边解码器。LinkNeighborLoader 每轮产生多个批次，所以在 Cora 上比一次全图前向更慢；它的价值是控制大图上的显存占用，而非保证小图加速。

### 4.3 MUTAG 图分类（30 轮）

| 模型 | Avg Test Acc | Max Test Acc | Min Test Acc |
|---|---:|---:|---:|
| GCN | 0.579 | **0.632** | 0.579 |
| GAT | 0.579 | 0.579 | 0.579 |
| GraphSAGE | 0.684 | 0.632 | **0.737** |
| GIN | 0.789 | **0.842** | 0.684 |

本次单次划分中 GIN + MaxPooling 最好，测试准确率为 84.2%。四个模型的平均结果中 MaxPooling 约为 67.1%，AvgPooling 约为 65.8%，MinPooling 约为 64.5%。池化与模型存在明显交互：GraphSAGE 使用 MinPooling 最好，而 GIN 使用 MaxPooling 最好，不能仅凭一种网络断言某个池化始终占优。MUTAG 测试集只有约 10% 的图，单次划分波动较大，正式结论应使用多随机种子或交叉验证。

### 4.4 toy KG 知识图谱补全（300 轮）

| 模型 | MRR | Hits@1 | Hits@3 | Hits@10 | Mean Rank |
|---|---:|---:|---:|---:|---:|
| TransE | 0.106 | 0.000 | 0.000 | 0.333 | 10.67 |
| RotatE | 0.101 | 0.000 | 0.000 | 0.500 | 10.50 |
| ConvE | **0.209** | 0.000 | **0.167** | **0.833** | **6.50** |

ConvE 在这组无泄漏测试上最好，但数据集仅有 15 个实体、3 个关系和 14 个训练三元组，只用于证明完整训练与 filtered ranking 流程可运行，不适合作为模型优劣的普遍结论。替换为 FB15k-237、WN18RR 等标准 TSV 数据后可复用同一脚本。

## 5. 学习率与网络层数消融

在 Cora 上固定 GCN、全图训练、隐藏维度 64、dropout 0.5，训练 30 轮：

| 学习率 | 层数 | Val Acc | Test Acc |
|---:|---:|---:|---:|
| 0.005 | 2 | 0.776 | 0.784 |
| 0.005 | 3 | 0.788 | 0.802 |
| 0.010 | 2 | 0.796 | 0.816 |
| 0.010 | 3 | 0.782 | 0.773 |
| 0.020 | 2 | **0.806** | **0.818** |
| 0.020 | 3 | 0.788 | 0.774 |

对两层 GCN，提高学习率从 0.005 到 0.02 带来持续提升；三层模型则在 0.005 时最好，较高学习率下性能下降。原因可能是更深模型具有更多非线性和传播步骤，对优化步长更敏感，也更容易出现过平滑。结果说明学习率与层数需要联合搜索，而不能独立选择。

## 6. 结论与限制

1. 节点分类和链路预测上，GCN 是当前配置下最稳定的基线；不同任务的最佳编码器并不相同。
2. 小图上全图训练通常更快；邻居采样面向的是更大图的内存扩展性，其速度收益需要在 Flickr 等规模上评估。
3. 图分类对池化方式敏感，MUTAG 上 GIN + MaxPooling 最佳，但数据集较小，应使用多次重复实验验证。
4. 两层 GCN 比三层配置更稳健，较深网络需要更小学习率或额外正则化。
5. 当前结果均为单随机种子，且 Flickr/ZINC 因外部数据源不可达未实跑；提交正式论文式结论前，应在数据下载恢复后补齐数据集矩阵，并报告多随机种子的均值与标准差。

原始指标文件位于各任务的 `results.json`，消融结果位于 `experiments/task1_ablation.json`。
