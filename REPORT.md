# 图神经网络 Beginner 实验报告

## 1. 环境与复现信息

- 运行环境：AutoDL `graph` Conda 环境，Python 3.11.16。
- 计算设备：NVIDIA RTX 4090 D，CUDA 可用。
- 主要依赖：PyTorch 2.7.1+cu128、PyTorch Geometric 2.8.0.post1、NumPy 2.4.6、scikit-learn 1.9.0。
- 采样扩展：pyg-lib 0.5.0、torch-scatter 2.1.2、torch-sparse 0.6.18，均与 PyTorch 2.7/CUDA 12.8 对应。
- `pip check` 无依赖冲突；`python self_check.py` 输出 `SELF_CHECK_OK`。
- 所有实验固定随机种子为 42，自动使用 CUDA，指标保存为 JSON。

快速复现：

```bash
conda activate graph
python self_check.py
python self_check.py --device cpu
bash run_quick_experiments.sh
python run_ablation.py --epochs 30
python run_batch_ablation.py --epochs 30
```

## 2. 方法与实现

| 任务 | 数据与划分 | 模型/方法 | 指标 |
|---|---|---|---|
| 节点分类 | Cora、Citeseer、Flickr 的官方掩码 | GCN、GAT、GraphSAGE、GIN；全图与 NeighborLoader | 验证/测试 Accuracy、耗时 |
| 链路预测 | RandomLinkSplit 划分训练/验证/测试边并生成负边 | 四种 GNN 编码器、点积解码器；全图与 LinkNeighborLoader | 验证/测试 ROC-AUC、耗时 |
| 图分类/回归 | TUDataset 按 8:1:1 划分；ZINC 使用官方 split | 四种 GNN；Avg/Max/Min 全局池化 | Accuracy 或 MAE、耗时 |
| 知识图谱补全 | 独立 train/valid/test TSV | TransE、RotatE、二维 ConvE；头/尾实体负采样 | 双向 filtered MRR、Hits@K、Mean Rank |

ConvE 将头实体和关系嵌入分别重排为二维网格，沿高度拼接后执行 `Conv2d`，再将卷积特征投影回嵌入维度并与尾实体嵌入做点积。二维形状由嵌入维度的因数自动确定；质数和很小的维度也使用自适应卷积核保持 shape 合法。知识图谱评测同时计算 `(?, r, t)` 与 `(h, r, ?)`，过滤训练、验证和测试集中其余已知真三元组。内置 toy KG 的测试三元组不出现在训练集中，避免数据泄漏。

四个训练脚本的计时只覆盖训练循环。当前版本在计时开始前和训练结束后显式同步 CUDA，并在结果 JSON 中写入 `timing_protocol`；验证/测试时间不计入 `train_seconds`。首次算子缓存初始化仍可能影响一次测量，正式速度对比应先做独立预热并重复运行。

## 3. 已运行实验范围

用于模型对比的正式基线为：Cora/Citeseer 节点分类各 30 轮、Cora/Citeseer 链路预测各 30 轮、MUTAG/PROTEINS 图分类各 30 轮，以及 toy KG 知识图谱补全 300 轮。Task 3 还对 MUTAG 的批量大小 16、32、64、128 做了 30 轮对照。所有下表耗时均来自增加 CUDA 显式同步后的重新运行。

Flickr 的 Google Drive 数据源和 ZINC 的 Dropbox 数据源在本次 AutoDL 网络上连接超时，因此这两个数据集的代码与命令已提供，但没有将未完成的下载记作实验结果。Cora/Citeseer 改用可访问镜像下载原始 Planetoid 文件，数据格式仍由 PyG 官方处理器解析。

## 4. 实验结果

### 4.1 节点分类（30 轮）

#### Cora

| 模型 | 全图 Test Acc | 采样 Test Acc | 全图秒/轮 | 采样秒/轮 |
|---|---:|---:|---:|---:|
| GCN | 0.816 | **0.828** | 0.012409 | 0.003743 |
| GAT | 0.693 | 0.761 | 0.003537 | 0.004927 |
| GraphSAGE | 0.802 | 0.803 | **0.001692** | 0.003354 |
| GIN | 0.656 | 0.713 | 0.001893 | 0.003654 |

#### Citeseer

| 模型 | 全图 Test Acc | 采样 Test Acc | 全图秒/轮 | 采样秒/轮 |
|---|---:|---:|---:|---:|
| GCN | 0.700 | **0.705** | 0.013195 | 0.005484 |
| GAT | **0.705** | 0.612 | 0.005103 | 0.006774 |
| GraphSAGE | 0.681 | 0.690 | **0.002613** | 0.004857 |
| GIN | 0.605 | 0.570 | 0.003076 | 0.005137 |

GCN 是两组节点分类实验中最稳定的模型：Cora 采样训练达到最高测试准确率 0.828，Citeseer 上 GCN 采样与 GAT 全图并列 0.705。Cora 上四种模型的采样准确率均不低于对应全图结果；Citeseer 上这一趋势并不成立，说明采样效果依赖数据与模型。小图中 GraphSAGE/GIN 的全图训练每轮更快；邻居采样主要用于控制大图显存，并不保证在小图上加速。

### 4.2 链路预测（30 轮）

#### Cora

| 模型 | 全图 Test AUC | 采样 Test AUC | 全图秒/轮 | 采样秒/轮 |
|---|---:|---:|---:|---:|
| GCN | 0.744834 | 0.741705 | 0.013711 | 0.263325 |
| GAT | 0.504350 | 0.500000 | 0.003421 | 0.260005 |
| GraphSAGE | 0.722726 | **0.776628** | 0.003859 | **0.256573** |
| GIN | 0.641015 | 0.710576 | **0.002108** | 0.263305 |

#### Citeseer

| 模型 | 全图 Test AUC | 采样 Test AUC | 全图秒/轮 | 采样秒/轮 |
|---|---:|---:|---:|---:|
| GCN | 0.759749 | 0.739457 | 0.013032 | 0.339894 |
| GAT | 0.569175 | 0.500000 | 0.004246 | **0.326499** |
| GraphSAGE | **0.768603** | 0.742978 | **0.001673** | 0.339896 |
| GIN | 0.709025 | 0.716479 | 0.001929 | 0.344012 |

Cora 上 GraphSAGE 采样训练的 AUC 最高，为 0.776628；Citeseer 上 GraphSAGE 全图最高，为 0.768603。GIN 在两个数据集上均从采样训练获益，而 GAT 在当前点积解码器下表现接近随机，需要进一步调整注意力配置或边解码器。同步后的数据也清楚显示：在 Cora/Citeseer 这类小图上，LinkNeighborLoader 的采样与批处理开销远高于一次全图传播；其优势应在无法直接装入显存的大图上评估。

### 4.3 MUTAG 图分类（30 轮）

| 模型 | Avg Test Acc | Max Test Acc | Min Test Acc |
|---|---:|---:|---:|
| GCN | 0.579 | **0.632** | 0.579 |
| GAT | 0.579 | 0.579 | 0.579 |
| GraphSAGE | 0.684 | 0.632 | **0.737** |
| GIN | 0.789 | **0.842** | 0.684 |

本次单次划分中 GIN + MaxPooling 最好，测试准确率为 84.2%。四个模型的平均结果中 MaxPooling 约为 67.1%，AvgPooling 约为 65.8%，MinPooling 约为 64.5%。池化与模型存在明显交互：GraphSAGE 使用 MinPooling 最好，而 GIN 使用 MaxPooling 最好，不能仅凭一种网络断言某个池化始终占优。MUTAG 测试集只有约 10% 的图，单次划分波动较大，正式结论应使用多随机种子或交叉验证。

### 4.4 PROTEINS 图分类（30 轮）

| 模型 | Avg Test Acc | Max Test Acc | Min Test Acc |
|---|---:|---:|---:|
| GCN | 0.7768 | 0.7232 | 0.7500 |
| GAT | 0.7589 | 0.8036 | 0.7946 |
| GraphSAGE | 0.8036 | 0.3393 | 0.3304 |
| GIN | 0.8214 | **0.8393** | 0.8304 |

PROTEINS 上 GIN 的三个池化结果都较稳定，GIN + MaxPooling 以 0.8393 最佳。GraphSAGE 对池化方式非常敏感：AvgPooling 达到 0.8036，而 Max/MinPooling 明显下降。这与 MUTAG 的结果共同说明，模型与池化方式需要联合选择，不能只比较网络结构。

### 4.5 toy KG 知识图谱补全（300 轮）

| 模型 | MRR | Hits@1 | Hits@3 | Hits@10 | Mean Rank |
|---|---:|---:|---:|---:|---:|
| TransE | 0.1056 | 0.0000 | 0.0000 | 0.3333 | 10.6667 |
| RotatE | 0.1008 | 0.0000 | 0.0000 | 0.5000 | 10.5000 |
| 二维 ConvE | **0.1401** | 0.0000 | **0.1667** | 0.5000 | **9.3333** |

标准二维 ConvE 的 MRR、Hits@3 和 Mean Rank 在这次 toy 实验中最好。toy 数据仅有 15 个实体、3 个关系和 14 个训练三元组，只适合验证二维卷积训练及双向 filtered ranking 流程，不适合作为模型优劣的普遍结论。替换为 FB15k-237、WN18RR 等标准 TSV 数据后可复用同一脚本。

## 5. 学习率与网络层数消融

在 Cora 上固定 GCN、全图训练、隐藏维度 64、dropout 0.5，训练 30 轮：

| 学习率 | 层数 | Val Acc | Test Acc |
|---:|---:|---:|---:|
| 0.005 | 2 | 0.776 | 0.784 |
| 0.005 | 3 | 0.788 | 0.802 |
| 0.010 | 2 | 0.796 | 0.816 |
| 0.010 | 3 | 0.782 | 0.773 |
| 0.020 | 2 | **0.806** | **0.818** |
| 0.020 | 3 | 0.788 | 0.774 |

对两层 GCN，提高学习率从 0.005 到 0.02 带来持续提升；三层模型则在 0.005 时最好，较高学习率下性能下降。原因可能是更深模型具有更多非线性和传播步骤，对优化步长更敏感，也更容易出现过平滑。结果说明学习率与层数需要联合搜索，而不能独立选择。

## 6. Task 3 批量大小消融

在 MUTAG 上固定 GIN + MaxPooling、训练 30 轮，仅改变批量大小：

| Batch size | Test Acc | 同步后秒/轮 |
|---:|---:|---:|
| 16 | 0.7368 | 0.056894 |
| 32 | 0.6842 | 0.035198 |
| 64 | 0.7895 | 0.026739 |
| 128 | **0.8421** | **0.026504** |

批量增大后每轮耗时总体下降，但准确率并非单调变化；本次单种子实验中 batch size 128 同时取得最高准确率和最低每轮耗时。MUTAG 数据量很小，这一结果可能受单次划分和优化随机性影响，正式结论应增加随机种子重复实验。

## 7. 结论与限制

1. 节点分类中 GCN 最稳定；链路预测中 GraphSAGE 在 Cora 采样和 Citeseer 全图设置下分别取得最佳 AUC，任务间最佳编码器并不相同。
2. 小图上全图训练通常更快；邻居采样面向的是更大图的内存扩展性，其速度收益需要在 Flickr 等规模上评估。
3. 图分类对池化方式敏感；MUTAG 和 PROTEINS 都由 GIN + MaxPooling 取得最高测试准确率，但单次划分仍需多随机种子复核。
4. 两层 GCN 比三层配置更稳健，较深网络需要更小学习率或额外正则化。
5. 批量大小会同时影响优化结果和训练效率；MUTAG 上 batch size 128 在本次运行中最佳，但不能直接外推到更大数据集。
6. 当前正式结果仍为单随机种子；Flickr/ZINC 因外部数据源不可达未实跑。数据下载恢复后应补齐这两个数据集，并报告多随机种子的均值与标准差。

正式原始指标文件保存在各任务的 `results.json`；学习率/层数消融结果位于 `experiments/task1_ablation.json`，批量大小对照结果位于 `experiments/task3_batch_ablation.json`。`.gitignore` 排除数据、权重、缓存以及 quick/smoke 临时结果，但不排除上述正式 JSON 指标。
